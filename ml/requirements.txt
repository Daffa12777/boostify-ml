# ============================================================
# requirements.txt — Boostify ML Dependencies
# ============================================================
# Install semua: pip install -r requirements.txt
#
# Untuk Raspberry Pi, tambahkan flag:
# pip install -r requirements.txt --break-system-packages
# ============================================================

# Core ML
deepface
#tensorflow==2.15.0       # untuk laptop/PC
#tflite-runtime==2.14.0 # untuk Raspberry Pi (uncomment ini, comment tensorflow)

# Computer Vision

opencv-python-headless  # untuk Raspi (tanpa GUI)

# Numerik
numpy
scikit-learn

# Utilitas
Pillow
tqdm
matplotlib

# Penyimpanan embedding
# (pickle sudah built-in Python, tidak perlu install)

# ============================================================
# Catatan untuk Raspberry Pi:
# - Ganti tensorflow → tflite-runtime
# - Ganti opencv-python → opencv-python-headless
# - Tambahkan flag --break-system-packages
# ============================================================
