# ============================================================
# requirements.txt — Boostify ML (dlib / face_recognition)
# Install: pip install -r requirements.txt
# Raspi : pip install -r requirements.txt --extra-index-url https://www.piwheels.org/simple
# ============================================================

# Face recognition engine (dlib ResNet 128-d)
# → otomatis nyeret dlib + Pillow
face_recognition==1.3.0

# Computer Vision
opencv-python==4.9.0.80

# Numerik
numpy==1.26.4

# Kirim absen ke backend
requests==2.31.0

# Progress bar (cuma kepake pas train.py)
tqdm==4.66.2